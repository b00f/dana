sudo: required
before_install:
  - sudo apt-get install sqlite qtbase5-dev
  - sudo apt-get install festival-dev
  
# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc
  

# Build steps
script:
  - cd source
  - qmake 
  - make
