sudo: required
before_install:
  - sudo apt-get install festival-dev
  - sudo apt-get install libqt5-dev
  
# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc
  

# Build steps
script:
  - cd source
  - qmake 
  - make
